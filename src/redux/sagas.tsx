import { all } from "redux-saga/effects";
export default function* rootSaga() {
    yield all([
        // authSaga(),
        // stateSaga(),
        // productSaga(),
        // categorySaga(),
        // comboSaga(),
        // orderSaga(),
        // TableSaga(),
        // InvoiceSaga()
    ]);
}